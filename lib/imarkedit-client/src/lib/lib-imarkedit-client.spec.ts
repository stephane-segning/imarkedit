import { libImarkeditClient } from './lib-imarkedit-client';

describe('libImarkeditClient', () => {
  it('should work', () => {
    expect(libImarkeditClient()).toEqual('lib-imarkedit-client');
  });
});
