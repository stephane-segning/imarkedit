import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ssegning.mobile.imarkedit',
  appName: 'imarkedit',
  webDir: '../../dist/apps/imarkedit-app/capacitor',
};

export default config;
